name: plinedev
channels:
  - conda-forge
dependencies:
  - python >=3.12
  - ipykernel >=5.1
  - pip >=20.0
  - setuptools >=50.0
  - sphinx >=3.0
  - pytest >=6.0
  - pytest-cov >=2.0
  - pip:
      - twine >=3.0
      - wheel >=0.34
      - build >=0.7