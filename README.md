# pipeline_dev

test-pipeline for the dev repo. This small project:

- contains few basic functions,
- can be tested using pytest,
- has a setup.py and should be pip installable,
- bas a doc,

several workflows are implemented:

- code linting on push

